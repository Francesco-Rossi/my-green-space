import 'package:flutter/material.dart';

class SpecificPlantPage extends StatelessWidget {
  const SpecificPlantPage({super.key});

  @override
  Widget build(BuildContext context) {
    
    return Scaffold(
      appBar: AppBar(
        title: const Text('Specific Plant Page'),
      ),
      body: const Text("Specific plant page"),
    );
  } // end build.
} // end Homepage.